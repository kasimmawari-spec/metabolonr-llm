# MetabolonR-LLM

An LLM agent that replicates the MetabolonR metabolomics analysis pipeline.

## Project Structure
- tools/       → the 11 analysis functions (QC, imputation, PCA, etc.)
- tests/       → golden-file tests for each tool
- data/        → sample metabolomics datasets
- logs/        → session logs (JSON)
